"""WakaFlockaFlow: no-code post-acquisition spectral flow cytometry analysis.

Installed as the ``wakaflockaflow`` package (``backend`` maps to it via the
build's package-dir). The modules use flat top-level imports and are launched by
``wakaflockaflow.cli:main``, which puts the package directory on ``sys.path``.
"""

__version__ = "0.1.0"
